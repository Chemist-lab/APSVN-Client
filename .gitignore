# Те, що не пишеться руками, а кладеться в збірку.
#
# Разом це 37 МБ бінарників, які змінюються цілими файлами: git для такого не
# створений, і кожне оновлення рантайму назавжди осідало б в історії. Вони
# їдуть до художника у складі APSVN.zip, а не через клон репозиторію.
/runtime/
/vendor/
/svn/

# Побічне
__pycache__/
*.pyc
*.apsvn-part
error.log

# Результат збірки — його місце в релізі, а не в історії
/APSVN.zip
